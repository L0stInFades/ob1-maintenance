echo -n "addr_assign_type: "; cat /sys/class/net/${ie}/addr_assign_type 2>/dev/null; echo;
            echo -n "address: "; cat /sys/class/net/${ie}/address 2>/dev/null; echo;
            echo -n "addr_len: "; cat /sys/class/net/${ie}/addr_len 2>/dev/null; echo;
            echo -n "broadcast: "; cat /sys/class/net/${ie}/broadcast 2>/dev/null; echo;
            echo -n "carrier: "; cat /sys/class/net/${ie}/carrier 2>/dev/null; echo;
            echo -n "carrier_changes: "; cat /sys/class/net/${ie}/carrier_changes 2>/dev/null; echo;
            echo -n "dev_id: "; cat /sys/class/net/${ie}/dev_id 2>/dev/null; echo;
            echo -n "dev_port: "; cat /sys/class/net/${ie}/dev_port 2>/dev/null; echo;
            echo -n "dormant: "; cat /sys/class/net/${ie}/dormant 2>/dev/null; echo;
            echo -n "duplex: "; cat /sys/class/net/${ie}/duplex 2>/dev/null; echo;
            echo -n "flags: "; cat /sys/class/net/${ie}/flags 2>/dev/null; echo;
            echo -n "gro_flush_timeout: "; cat /sys/class/net/${ie}/gro_flush_timeout 2>/dev/null; echo;
            echo -n "ifalias: "; cat /sys/class/net/${ie}/ifalias 2>/dev/null; echo;
            echo -n "ifindex: "; cat /sys/class/net/${ie}/ifindex 2>/dev/null; echo;
            echo -n "iflink: "; cat /sys/class/net/${ie}/iflink 2>/dev/null; echo;
            echo -n "link_mode: "; cat /sys/class/net/${ie}/link_mode 2>/dev/null; echo;
            echo -n "mtu: "; cat /sys/class/net/${ie}/mtu 2>/dev/null; echo;
            echo -n "netdev_group: "; cat /sys/class/net/${ie}/netdev_group 2>/dev/null; echo;
            echo -n "operstate: "; cat /sys/class/net/${ie}/operstate 2>/dev/null; echo;
            echo -n "proto_down: "; cat /sys/class/net/${ie}/proto_down 2>/dev/null; echo;
            echo -n "speed: "; cat /sys/class/net/${ie}/speed 2>/dev/null; echo;
            echo -n "tx_queue_len: "; cat /sys/class/net/${ie}/tx_queue_len 2>/dev/null; echo;
            echo -n "type: "; cat /sys/class/net/${ie}/type 2>/dev/null; echo;
            echo -n "wireless: "; cat /proc/net/wireless 2>/dev/null | grep ${ie}; echo;
            echo -n "wirelessspeed: "; iw dev ${ie} link 2>&1 | grep bitrate; echo;